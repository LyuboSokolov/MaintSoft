@model IEnumerable<AssetViewModel>

@{
    ViewBag.Title = "Assets";
}
<a class="btn btn-success" style="margin-bottom: 10px" asp-area="" asp-controller="Asset" asp-action="Add">Add Asset</a>
@if (Model.LongCount() == 0)
{
    <div> Not Yet Assets!</div>
}
else
{

    <table class="table table-bordered table-responsive table-hover table-striped">
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Status</th>
            <th>Person responsible</th>
        </tr>
        @foreach (var asset in Model)
        {
            <tr>
                <td>@asset.Name</td>
                <td>@asset.Description</td>
                <td>@(asset.IsAvailable == true ? "Available" : "End of life")</td>
                <td>@asset.PersonResponsible</td>
            </tr>
        }

    </table>
}